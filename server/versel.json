{
    "version": 2,
    "name": "nest-next-app-server",
    "builds": [
        {
            "src": "./dist/main.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "./dist/main.js"
        }
    ]
}
